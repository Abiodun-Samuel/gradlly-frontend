export const OTJ_PATHS = Object.freeze({
  BASE: "/api/v1/otj-log-entries",
  BULK_APPROVE: "/api/v1/otj-log-entries/bulk-approve",
});

export const OTJ_STATUS = Object.freeze({
  SUBMITTED: "submitted",
});

export const OTJ_STATUS_LABELS = Object.freeze({
  draft: "Draft",
  submitted: "Submitted",
  approved: "Approved",
  rejected: "Rejected",
});
