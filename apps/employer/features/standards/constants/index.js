export const STANDARD_PATHS = Object.freeze({
  LIST: "/api/v1/standards",
  detail: (id) => `/api/v1/standards/${id}`,
});

export const PROGRAMME_PATHS = Object.freeze({
  LIST: "/api/v1/programmes",
  detail: (id) => `/api/v1/programmes/${id}`,
});
