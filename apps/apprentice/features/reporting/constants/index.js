export const REPORTING_PATHS = Object.freeze({
  LEARNER_SUMMARY: "/api/v1/learners/me/summary",
});

export const OTJ_PACE_LEVEL = Object.freeze({
  ON_TRACK: "on_track",
  AT_RISK: "at_risk",
  OFF_TRACK: "off_track",
});

export const OTJ_PACE_LABELS = Object.freeze({
  on_track: "On track",
  at_risk: "At risk",
  off_track: "Off track",
});
